namespace TmTech_v1.Utility
{
    public enum CRUD { Insert, Update };
    public enum ApproveStatus { Wait = 1, Approved = 2, Cancel = 3 };
    public enum ProgressStatus { Waiting = 1, InProgress = 2, Finish = 3, Cancel = 4 };
    public enum UserTypeFlag { Staff = 1, Customer = 2, Developer = 3 };
}
